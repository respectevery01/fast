import type { CarbonTheme } from '../CarbonTheme.js';
import { type Ref } from 'vue';
import type { Header } from 'vitepress';
export type MenuItem = Omit<Header, 'slug' | 'children'> & {
    element: HTMLHeadElement;
    children?: MenuItem[];
};
export declare function resolveTitle(theme: CarbonTheme.Config): string;
export declare function getHeaders(range: CarbonTheme.Config['outline']): MenuItem[];
export declare function resolveHeaders(headers: MenuItem[], range?: CarbonTheme.Config['outline']): MenuItem[];
export declare function useActiveAnchor(container: Ref<HTMLElement>): void;
